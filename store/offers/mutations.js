export default {
  getFireInsOffer(state, offers) {
    state.offers = offers
  },
  getThirdPartyOffer(state, offers) {
    state.offers = offers
  },
  getCarBodyOffer(state, offers) {
    state.offers = offers
  },
}
